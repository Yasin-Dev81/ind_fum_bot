

def register(aioscheduler):
    # aioscheduler.add_job(backup_mysql_database, "interval", seconds=5)
    # aioscheduler.add_job(trash_cleaner, 'cron', hour=2, minute=30)
    # aioscheduler.add_job(price_notif, 'cron', hour=20, minute=0)
    # aioscheduler.start()
